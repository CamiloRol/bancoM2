class Cliente {
    constructor(nombre, apellido, direccion, identificacionCC, contrasena) {
        this.nombre = nombre
        this.apellido = apellido
        this.direccion = direccion
        this.identificacionCC = identificacionCC
        this.contrasena = contrasena
    }
}

export default Cliente